//pagina con tutte le destinazioni divise per continente, con la possibilità di aggiungere nuove destinazioni (solo per admin)
import { useAuth } from '../context/auth';
function DestinationSection() {
    return (
        <div className="px-10 py-10">
            <p className="text-mytheme-text">Pagina in costruzione...</p>
        </div>
    );
}

export default DestinationSection;